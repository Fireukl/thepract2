Как использовать программу:
1. С помощью консоли перейти в каталог программы.
2. Создать каталог build (mkdir build), затем перейти в него (cd build).
3. После захода в папку build необходимо написать cmake .., а затем make.
4. После написания make необходимо написать ./thepract2 -"необходмые параметры".


Доступные параметры работы программы:
-h, --help - помощь (вывод всех доступных команд)
-p, --processes - процессы (вывод всех запущенных процессов)
-u, --users - пользователи (вывод всех пользователей)
-l, --log [PATH] - вывод параметра заданного перед этим параметром в файл
-e, --errors [PATH] - ошибки (вывод ошибок)
